extends layout

block content
  #main
    h1(id="welcome", class="alt-font") TRIVIA<br>CHALLENGER!
    hr
    form(id="start-form", action="/selection", method="post")
      label(id="name-label", for="name") Your Name
      br
      input(id="name", type="text", name="name")
      p You have 1 minute to answer as many questions as possible.<br>Good luck!
      input(class="submit", type="submit", value="START")
